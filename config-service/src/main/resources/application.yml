server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-repo/microservices-config
          clone-on-start: true
          search-paths: '{application}'